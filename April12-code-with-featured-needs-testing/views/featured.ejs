<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Featured Resorts</title>
        <style>
            html {
                position: relative;
                min-height: 100%;
            }
            body {
                font-family: 'Arial', sans-serif;
                background-color: #F2E3D5; /* cream background */
                color: #027373; /* dark teal text for readability */
                margin: 0;
                padding: 0;
                margin-bottom: 60px; /* Height of the footer */
            }
            header {
                background-color: #04ADBF; /* teal background */
                color: #fff; /* white text */
                padding: 20px;
                text-align: center;
            }
            nav ul {
                list-style: none;
                padding: 0;
                text-align: center;
                margin-bottom: 0;
            }
            nav li {
                display: inline;
                margin-right: 20px;
            }
            nav a {
                text-decoration: none;
                color: #fff;
                font-weight: bold;
            }
            nav a:hover {
                color: #04BF8F; /* green color for hover state */
            }
            h1 {
                margin: 0;
                font-size: 2.5rem;
            }
            p {
                font-size: 1.2rem;
            }
            main {
                padding: 30px 20px 20px 20px;
            }
            section {
                margin-top: 30px;
            }
            article {
                background-color: #fff; /* white background */
                padding: 20px;
                margin-bottom: 20px;
                border-radius: 8px;
                box-shadow: 0 4px 8px rgba(0,0,0,0.1);
            }
            h3 {
                color: #04ADBF; /* teal color for headings */
            }
            footer {
                background-color: #04ADBF; /* teal background */
                color: #fff; /* white text */
                text-align: center;
                padding: 10px;
                position: absolute;
                bottom: 0;
                width: 100%;
                height: 60px; /* Height of the footer */
            }
            footer p {
                margin: 0;
            }
        </style>
    </head>
<body>
        <header>
            <nav>
                <ul>
                    <li><a href="/">Home</a></li>
                </ul>
    <h1>Explore Our Featured Resorts</h1>
    <% if (resorts && resorts.length > 0) { %>
        <ul>
            <% resorts.forEach(resort => { %>
                <li>
                    <h2><%= resort_name %></h2>
                    <p><strong>Location:</strong> <%= city %>, <%= country %></p>
                    <p><strong>Type:</strong> <%= resort_type %></p>
                    <p><strong>Summary:</strong> <%= summary %></p>
                    <p><strong>Cost:</strong> <%= cost %></p>
                    <p><strong>Amenities:</strong> <%= amenities %></p>
                    <!-- add more details and make prettier if needed -->
                </li>
            <% }); %>
        </ul>
    <% } else { %>
        <p>Sorry, no featured resorts found.</p>
    <% } %>
     <footer>
        <p>&copy; 2024 Travel Guru. All rights reserved.</p>
    </footer>
</body>
</html>